# AIえほんメーカー

子ども向けの絵本を、入力した設定から自動生成するWebアプリ。
GitHub Pages で公開している。

## 構成

ビルド不要の静的サイト。ファイルは実質この2つだけ。

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体。HTML・CSS・JavaScript を1ファイルに全部入れている |
| `.github/workflows/static.yml` | `main` に push すると GitHub Pages へ自動デプロイ |

`.nojekyll` は GitHub Pages の Jekyll 処理を止めるための空ファイル。消さないこと。

## 動き方

1. ユーザーがブラウザで Google Gemini の API キーを入力する
2. 設定（主人公・舞台・雰囲気・絵のタッチ・ページ数）をもとにプロンプトを組み立てる
3. Gemini の `generateContent` を直接呼び、絵本のページデータを受け取る
4. ページをめくる形で表示する

API キーは**ブラウザ上でユーザーが入力するもの**で、リポジトリには含まれない。
この前提を壊さないこと（キーをコードに書き込む・どこかへ送信する変更は入れない）。

利用モデルは `MODEL_CANDIDATES` に候補を並べ、使えるものを順に試す方式。
モデルが使えなくなったらこのリストを直す。

## 変更するときのルール

- `index.html` 1ファイル完結の構成を維持する。ビルドツールやフレームワークは入れない。
- 外部ライブラリの追加は避ける。どうしても必要なら理由を説明してから。
- 表示テキストはすべて日本語。子どもと保護者が読む画面なので、やさしい言葉づかいにする。
- スマホでも崩れないこと。変更後は幅400px程度でも確認する。
- 色は `:root` の CSS変数（`--bg` `--main` など）を使う。個別に色を直書きしない。

## 確認方法

ローカルで開くだけで動く。

```
start index.html
```

デプロイは `main` への push で自動。作業ブランチに push しただけでは公開されない。

---

## Instagram運用:役割分担ルール

### ClaudeCode(自分)の担当
- 伸びているリールのリサーチと、数値の集計
- 台本の構成づくりと執筆
- 動画のカット編集、字幕入れ、書き出し
- 公開前の最終チェック(事実確認、誤字、尺)

### Codexに任せること
- サムネイルや挿入画像の生成
- 時間のかかる実装作業と、コードのレビュー

### 進め方のルール
- 画像が必要になったら自分で作らず、必ずCodexに投げる
- 重い実装は `/codex:rescue`、書いたものの点検は `/codex:review` を使う
- Codexから返ってきたものは、自分が目を通してからユーザーに見せる
- 確認できない数字や事実は断定せず、「要確認」として残す
- ユーザーへの報告は日本語で、専門用語には補足をつける
